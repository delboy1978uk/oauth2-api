actor: ApiTester
modules:
  enabled:
  - REST:
      url: https://apache
      depends: PhpBrowser
      part: Json
env:
  travis:
    modules:
      enabled:
      - REST:
          url: http://awesome.scot:8000
          depends: PhpBrowser
          part: Json